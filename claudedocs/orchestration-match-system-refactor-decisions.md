# Orchestration deviation log — match-system-refactor

Run start commit: `bc257dda`
Date: 2026-06-25

This log records decisions made during orchestrated execution that were **not** spelled out in the plan, plus any unresolved issues left after the bounded patch loop.

## Format

`- [MSR-XX] decision — one-line rationale`

## Entries

- [MSR-01] `MatchReviewSummaryResult` defined in domain `types.ts` alongside the existing server-local `MatchReviewSummaryResult` in `match-review-queue.functions.ts` — two types with the same name exist in different modules temporarily; the story's compile-safe-adapter clause permits this until a later story migrates callers.
- [MSR-03] All existing callers of `review(accountId, orientation)` and `summary(accountId, orientation)` now pass `'song'` explicitly (song-only app state) rather than using a root prefix key — keeps invalidation scoped to the currently-active orientation and avoids invalidating playlist-orientation keys before they exist.
- [MSR-02] `maxScore` accumulation in `deriveUndecidedSongsForQueue` was also migrated to use `strictnessScore(mr)` (not only the filter line) — maxScore drives queue ordering so it must reflect the same quality signal shown to the user, not the legacy ordering score.
- [MSR-02] `deriveUndecidedSongs` in `src/lib/server/matching.functions.ts` still reads `mr.score` directly for both strictness filter and maxScore — remaining caller to migrate in a later story (MSR-12 or MSR-18).
- [MSR-04] Playlist orientation rerank instruction string invented as "Given a song's mood and themes, judge if this playlist is a good home for it." — the plan specifies the map key `RERANK_INSTRUCTION_BY_ORIENTATION` but does not specify the playlist instruction text; MSR-13/14 can override the string if a better one is decided before that story lands.
- [MSR-05] Migrations `20260623170000` and `20260623180000` were applied to the local DB out-of-band but absent from `supabase_migrations.schema_migrations`; manually inserted tracking rows before applying the MSR-05 migration to unblock `supabase migration up`.
- [MSR-05] `publish_match_snapshot` body required no functional changes for the compatibility shell — PostgreSQL's JSONB field access already silently ignores unknown keys (including `rankings`), so `CREATE OR REPLACE` adds only an explicit comment pinning that behavior as intentional until MSR-17.
- [MSR-06] RPC lifecycle-state updates extracted to a separate migration `20260625020000_msr06_rpc_lifecycle_state.sql` because the state check constraint migration (`20260625010000`) was already applied before the RPC conflict was discovered; splitting avoids a db reset and keeps each migration unit atomic.
- [MSR-06] `updateQueueItemResolved` in `queries.ts` renames the `state` parameter to `_legacyState` and always writes `state = 'resolved'` — the old distinct terminal states (`completed`, `skipped`, `unavailable`) are now encoded only in the `resolution` column; the parameter is kept for caller compatibility until a later story removes it.
- [MSR-06] `fetchQueuedSongIds` and `fetchPendingSongIds` in `queries.ts` filter out nullable `song_id` values with a type-guard rather than exposing `string | null` to callers — playlist-mode items in these sessions would have no song_id; callers using these functions are song-mode-only today.
- [MSR-06] `mapItemRow` and the server-function mapper both use `row.song_id as string` (non-null assertion) for the legacy `MatchReviewQueueItem.songId: string` field — both mappers are on the song-mode path where `song_id` is always set; later stories replace this with `MatchReviewSubject`.
- [MSR-06] `insertQueueItems` upsert still uses `onConflict: "session_id,song_id"` which referenced the dropped `idx_match_review_queue_item_session_song` index; the new partial index `idx_match_review_queue_item_session_song_subject` is `WHERE orientation = 'song'` and Supabase JS does not support partial-index conflict targets — this is a deferred follow-up for MSR-18/MSR-19 when the upsert path is replaced with orientation-aware insertion.
- [MSR-06] Issue 1 choice: used RPC approach (option b) — created `insert_queue_song_items` RPC with explicit partial-index WHERE clause, updated `insertQueueItems` to call `.rpc()`. Verified function exists in DB (`pg_proc` lookup). RPC returns void; `appendedCount` is still derived from `ordered.length` in the service, not from the RPC return value.
- [MSR-06] Known defect (MSR-26–28): `20260625020000_msr06_rpc_lifecycle_state.sql:54` uses `v_item.song_id` without null guard — playlist items (song_id IS NULL) will produce wrong entitlement results. Deferred to MSR-26–28 RPC rewrites.
- [MSR-07] RPC external parameter `p_served_rank` kept unchanged in `add_match_review_item_decision_atomic` — the parameter is a public RPC contract that will be revised in MSR-23 when the full add-decision signature is replaced; only the internal DB column write was updated to `model_rank`.
- [MSR-07] `DismissQueueItemAtomicDecision.servedRank` renamed to `modelRank` and dismiss JSON key renamed from `served_rank` to `model_rank` — the `jsonb_to_recordset` alias was updated to match so the RPC, JSON contract, and TS interface are consistent; this is a breaking change to the dismiss RPC JSONB field name (all callers migrated in the same story).
- [MSR-07] `match_review_item_visible_pair` uses a composite PK of `(queue_item_id, song_id, playlist_id)` — enforces at most one visible-pair row per pair per card; unique visible rank per card is enforced separately by `idx_match_review_item_visible_pair_queue_visible_rank`.
- [MSR-08] `claim_pending_library_processing_job` RPC updated with `available_at <= now()` predicate despite story spec marking "Claim RPC predicate changes" as out of scope — the task instructions explicitly required this minimal change; behavior stays compat (all existing jobs default available_at = now() so nothing is deferred by the addition).
- [MSR-08] `idx_job_library_processing_poll` index recreated with `available_at ASC` as the leading key — leading with the range column lets the planner scan only the ready slice before sorting by priority, which is the access pattern the updated claim RPC uses.
- [MSR-08] `onboarding-session.test.ts` fixture extended with `match_view_mode: "song"` — the new NOT NULL column is now required in the `UserPreferences` type; existing fixture lacked the field and TypeScript correctly flagged it.
- [MSR-06/07] test regression fix: old terminal states `"completed"`, `"skipped"`, `"unavailable"` in test fixtures replaced with `"resolved"`; in-progress `"presented"` replaced with `"active"`; `servedRank` expectation in `dismissMatchReviewItem` call assertions replaced with `modelRank` — all stale against the new `pending|active|resolved` lifecycle and the MSR-07 `DismissQueueItemAtomicDecision.modelRank` rename; no production bugs found, all 7 failures were fixture drift.
- [MSR-12] `retainStoredMatchPairs` placed in new `src/lib/domains/taste/song-matching/retention.ts` rather than added to `service.ts` — keeps the pure retention helper isolated from the `MatchingService` class and avoids making `service.ts` import `DEFAULT_MATCHING_CONFIG` a second time just for the constants; file is adjacent to `service.ts` per spec touchpoints.
- [MSR-13] `ANALYSIS_TAIL_MAX_CHARS` exported from `match-ranking.ts` (where the builders live) rather than shared via import from `reranking.ts` — `reranking.ts` keeps its own private copy for now; the constant is identical (1600) and MSR-14/15 will migrate `reranking.ts` to use the shared builders, at which point the private copy can be removed.
- [MSR-13] `buildPlaylistRerankDocument` always returns `'metadata'` document mode — playlists have no separate analysis prose in the current schema; the mode field is returned for consistency with `buildSongRerankDocument` and to allow MSR-14/15 to store it in `match_result_ranking.document_mode` without special-casing the playlist path.
- [MSR-09] `latestRequestedAt` and `mergeMatchRefreshProgress` extracted to `src/lib/platform/jobs/match-refresh-merge.ts` — the merge helpers needed by `ensureMatchSnapshotRefreshJob` are pure functions; extracting them into a dedicated module enables direct unit testing without mocking the Supabase client builder chain.
- [MSR-09] `ensureMatchSnapshotRefreshJob` loop bumped from 2 to 3 iterations — the new pending-job update path adds a third distinct state machine step (running-return | pending-update | insert) and the update can produce a null if the job is claimed mid-loop, so a third attempt is needed to guarantee the caller always gets a result without an extra top-level retry.
- [MSR-09] `MATCH_REFRESH_DEBOUNCE_MS_BY_CHANGE` keys are the exact `LibraryProcessingChange["kind"]` discriminants rather than the plan's more granular sub-categories (`library_synced_target_metadata`, `playlist_management_session_flushed_recompute`) — the current type has no sub-kind split; `library_synced` gets 0 ms so membership changes are never delayed; `playlist_management_session_flushed` gets 8 s to cover the playlist editor config-save case.
- [MSR-06] `insert_queue_song_items` search_path hardening — original migration `20260625030000` created the function `SECURITY DEFINER` without `SET search_path`, failing the security-invariants CI guard. New migration `20260625060000_msr06_harden_insert_queue_song_items.sql` re-creates the function with `SET search_path = public` and adds the standard `REVOKE … FROM PUBLIC, anon, authenticated` / `GRANT … TO service_role` hardening to match all sibling SECURITY DEFINER RPCs.
- [MSR-06] `queue-helpers.test.ts` fixture drift — `makeItem` state type and three test fixtures still used the old lifecycle values (`"presented"`, `"completed"`, `"skipped"`, `"unavailable"`). Updated `makeItem` type union to `"pending" | "active" | "resolved"`, replaced `"presented"` with `"active"`, replaced old terminal states with `"resolved"`, and renamed the test title to "returns only pending and active items, sorted by position". Production `deriveUnresolvedIds` was correct; all failures were fixture drift only.
- [MSR-10] `isMatchRefreshJobSuperseded` implemented as a pure 2-argument function `(job, latestRequestedAt)` rather than the plan's async `(accountId, jobId, satisfiesRequestedAt)` form — the pure form is directly testable without DB mocks; the async state-query is encapsulated in the local `checkIfSuperseded` helper inside the orchestrator, keeping I/O at the call site.
- [MSR-10] Checkpoint 5 placed after building `resultEntries` and before `publishSnapshot` (not before `resultEntries` construction as the spec's "before publish" could imply) — building result entries is cheap CPU work; the expensive I/O is the publish RPC call itself, so the checkpoint correctly guards the publish boundary.
- [MSR-10] `executeMatchSnapshotRefresh` return type changed from `MatchSnapshotRefreshResult` to `MatchSnapshotRefreshOutcome` discriminated union — callers in `execute.ts` and orchestrator tests updated; `orchestrator-exclusions.test.ts` assertions updated to access `.result.published` via the new outcome shape.
- [MSR-11] `isMatchSnapshotMeasurementSuperseded` checks only `workflow === "match_snapshot_refresh"` and `outcome === "superseded"` with no `details` requirement — the runner writes a superseded measurement with `details: undefined` (no payload), so requiring details would cause a spurious conservative fallback; the outcome string alone is sufficient to identify the superseded path.
- [MSR-14] `StoredMatchPairForRanking` interface introduced in `match-ranking.ts` rather than importing full `MatchResult` — only `songId`, `playlistId`, `fusedScore` are needed; the structural subset keeps the ranking module decoupled from the full domain type and avoids a cross-layer import from workflows into taste/song-matching.
- [MSR-14] `reranking.ts` private `ANALYSIS_TAIL_MAX_CHARS = 1600` constant removed and replaced with import of `buildSongRerankDocument` from `match-ranking.ts` — MSR-13 deferred this migration; MSR-14 includes it as specified in the task scope ("remove the duplicate private copy").
- [MSR-14] `documentMode` for song-orientation ranked pairs is always `'metadata'` — `buildPlaylistRerankDocument` (which builds the candidate documents) always returns `'metadata'` since playlists have no analysis prose; this is consistent with MSR-13's note that the field propagates the builder output unchanged.
- [MSR-14] Partial rerank tail detection uses `typeof candidate.metadata?.rerank_score === 'number'` — `RerankerService` sets `metadata.rerank_score` only on candidates that went through the cross-encoder; tail candidates (below threshold or beyond topN) carry no metadata; this is the only stable signal available without changing the service contract.
- [MSR-15] `isSuperseded` added as an optional parameter to both `rankSongSuggestionLists` and `rankPlaylistSuggestionLists` rather than being enforced only at the combined entry point — the spec says the checkpoint is "between suggestion lists" which requires the check inside the per-subject loops, not only between orientation calls; optional keeps the existing tests unchanged.
- [MSR-15] `rankMatchSuggestionLists` returns `RankMatchSuggestionListsResult { status, byOrientation }` rather than `Map<MatchOrientation, RankedSuggestionLists[]>` — a discriminated result is needed so MSR-17's publish path can detect a mid-run superseded stop without a separate re-check after the call returns.
- [MSR-15] `documentMode` for playlist-orientation ranked pairs reflects the song candidate document mode (analysis or metadata depending on `SongForRanking.analysisText`) — this is the converse of song orientation where candidate playlists always produce 'metadata'; storing the actual mode enables MSR-16/17 to hash and publish the correct document_mode per pair.
- [MSR-16] `hashRankingConfig` placed in `hashing.ts` alongside the other domain hash helpers rather than in `match-ranking.ts` — keeps all hash primitives in one module (hashing.ts already owns all other snapshot component hashes); `cache.ts` imports from both modules to wire the ranking constants into `computeMatchSnapshotMetadata`.
- [MSR-16] kept `rankingConfigHash` optional in `hashMatchSnapshot` (consistent with sibling hash fields; sole caller always passes it; the unit test exercises the without-hash case) — accepted forward-risk, MSR-17 must continue to pass it.
- [MSR-17] `RankingRowPayload` carries an index signature `[key: string]: Json` alongside its named fields — required so the type is directly assignable to the `Json` type expected by the `publish_match_snapshot` RPC parameter; all specific field types (`string`, `number`, `number | null`) are subsets of `Json` so the index signature is sound.
- [MSR-17] Legacy score/rank mirror set in the orchestrator (TS) rather than in the SQL RPC — the orchestrator maps song-orientation orderingScore/rank onto the result entry's `score`/`rank` before passing to `writeMatchSnapshot`; this keeps all ranking-to-legacy translation at the call site and avoids adding UPDATE logic to the RPC body.
- [MSR-17] `retainStoredMatchPairs` wired in the same story — the spec says "after retained pair computation" implying retention was already wired; it was not (prior orchestrator used raw `matchResult.value.matches`), so MSR-17 adds the retention step as part of the spec's "wire rankMatchSuggestionLists after retained pair computation" requirement.
- [MSR-17] `rerankerService ?? new RerankerService()` used when `rerankerService` is undefined — the `RerankerService` constructor only parses config via Zod and cannot throw; the guard exists in the orchestrator from an earlier version when the constructor could fail; ranking always runs with a real service instance so fused_fallback degradation applies naturally if the provider is actually unavailable.
- [MSR-17] Analysis fetch gated on `storedPairs.length > 0` rather than `rerankerService && matches.size > 0` — the analysis text is needed for ranking document construction regardless of reranker availability (songs with analysis produce 'analysis' mode documents); gating on storedPairs avoids the fetch on empty-match paths.
- [MSR-17] FOLLOW-UP (non-blocking, flagged in review): orchestrator.ts ~line 600 `rerankerService ?? new RerankerService()` runs uncaught while construction at ~line 270 is wrapped in try/catch; the inline comment's 'cannot throw' claim is misleading. Reranker construction failure would crash the orchestrator instead of degrading. Fix the comment or wrap line 600 in the same try/catch.
- [MSR-18] `mapItemToDto` added as an exported function in `queries.ts` while `mapItemRow` is kept private — existing callers that use `MatchReviewQueueItem` (the legacy song-only type) are unchanged; only `fetchOwnedQueueItem` in `match-review-queue.functions.ts` migrates to `MatchReviewQueueItemDto` in this story.
- [MSR-18] `mapItemToDto` provided as an inline implementation inside the `vi.mock` factory in `match-review-queue.functions.test.ts` rather than via `vi.importActual` — the function is pure (no DB calls) so an inline mapper avoids the async mock factory pattern while keeping test data paths correct.
- [MSR-18] Song-mode server functions (`getMatchReviewItem`, `markMatchReviewItemPresented`, `addSongToPlaylistFromQueueItem`, `dismissMatchReviewItem`) now guard for `item.subject.orientation !== 'song'` and return early with an appropriate failure — playlist-mode UI is out of scope for MSR-18 so these guards are stubs that unblock type-safe compilation without silently ignoring non-song items.
- [MSR-18] `state: "completed"` fixture in `match-review-queue.functions.test.ts` updated to `state: "resolved"` — pre-existing drift from MSR-06 B9-C lifecycle split; `mapItemToDto` calls `toLifecycleState` which would throw for the old value; the assertion was also updated from `"completed"` to `"resolved"` to reflect the current lifecycle model.
- [MSR-18] No new migration added — all required schema changes (session.orientation, queue_item.orientation, playlist_id column, exactly-one-subject constraint, per-orientation unique index) were applied in MSR-06 migration `20260625010000_msr06_queue_session_orientation.sql` and types were already regenerated.
- [MSR-19] `computeVisibilityConfigHash` uses simple string concatenation (`vc_${orientation}_${minScore}_${readTimeFiltersHash}`) rather than SHA-256 — the hash is used only for idempotency keying, not collision resistance; the inputs are already structured and deterministic, so a crypto hash adds no correctness value here.
- [MSR-19] `QueueVisibilityConfigHashInput` and `OrderedSubject` placed in `types.ts` rather than `service.ts` — they are domain types needed by MSR-20/22 callers; co-locating them with the other queue domain types avoids a service-layer import in future callers.
- [MSR-19] `fetchAppliedSnapshotIds` now selects both `snapshot_id` and `visibility_config_hash` and returns composite `${snapshotId}:${hash}` keys — the call signature is unchanged but the set contents changed; all callers are within `service.ts` and the unit test was updated to pass composite keys.
- [MSR-19] `appendSnapshotDelta` still uses song-mode-only `fetchQueuedSongIds` and `insertQueueItems` for the post-derivation filter/insert steps — playlist-mode insertion via RPC is not yet implemented; only the derivation (steps 3+4+7) is now orientation-aware via `getOrderedUndecidedSubjects`.
- [MSR-19] No new migration — `visibility_config_hash` column and composite PK `(session_id, snapshot_id, visibility_config_hash)` were already added in `20260625010000_msr06_queue_session_orientation.sql`; the column had a `DEFAULT 'legacy'` for rows written before MSR-19; new writes now pass the computed hash explicitly.
- [MSR-20] `resolveMatchReviewSummary` signature updated to `(accountId, orientation)` — the exported helper is called by `dashboard.functions.ts` (which passes `"song"`) and `getMatchReviewSummary` server fn (which passes `data.orientation`); adding the parameter to the helper rather than only the server fn keeps the signature consistent and type-checked at all three call sites.
- [MSR-20] `syncActiveMatchReviewSessions` iterates orientations sequentially via a for-loop rather than Promise.all — sequential ensures DB write ordering matches orientation order in the result array and avoids two concurrent row-level locks on the same session; throughput impact is negligible since this runs in a background completion effect.
- [MSR-20] `dashboard.functions.ts` call sites for `resolveMatchReviewSummary` pass `"song"` explicitly — dashboard surfaces are song-mode-only until preferred-summary delegation lands (out of scope for MSR-20); hard-coding `"song"` is the same orientation that was previously implicit.
- [MSR-20] No new migration — all server function changes are TypeScript/query-key contract only; no schema additions required.
- [MSR-21] `MatchViewMode` defined as a local type alias in `preferences-queries.ts` rather than imported from `@/features/matching/types` — keeps the lib/domains layer free of features-layer imports; the types are structurally identical (`'song' | 'playlist'`) so assignability is preserved.
- [MSR-21] `orientation: MatchOrientation` added to `MatchReviewSummaryResult` — all three callers (sidebar, dashboard, preferred summary) need the orientation to build the correct Match link without a separate preference query; explicit orientation summaries return their own orientation; the field is informational and does not break any existing assertion.
- [MSR-21] Sidebar Match nav item rendered inline with direct `Link` rather than through `NavItem` — NavItem's `to: string` prop cannot carry TanStack Router's typed search params (`{ mode?: "playlist" }`); inlining one item with matching CSS is safer than a generic `search?: Record<string, string>` bypass in NavItem that could silently accept invalid params for other routes.
- [MSR-21] `dashboard.functions.billing.test.ts` fixture drift fixed: `mockResolveMatchReviewSummary` renamed to `mockResolvePreferredMatchReviewSummary` and call-arg assertions updated from `("acct-1", "song")` to `("acct-1")` — dashboard now calls `resolvePreferredMatchReviewSummary` (no orientation arg); all assertions were stale against the new preferred-summary contract.
- [MSR-22] `MatchPairInput` and `RankingInput` exported from `visible-suggestion-list.ts` rather than kept internal — the pure `deriveVisibleSuggestions` function is the primary test surface and callers (MSR-23/24) will pass these shapes directly; exporting keeps tests clean without an `as any` cast.
- [MSR-22] Unranked pairs receive a synthetic modelRank starting at `maxRankedModelRank + 1` rather than being dropped — dropping unranked pairs would hide valid matches on old snapshots that predate ranking data; synthetic ranks keep the full visible set contiguous and deterministic.
- [MSR-22] `getMatchPairsForSong` / `getMatchPairsForPlaylist` / `getMatchRankingsForSong` / `getMatchRankingsForPlaylist` added to `song-matching/queries.ts` rather than a new file — these are natural extensions of the existing pair-query surface in that module; no new file needed per the likely-touchpoints list.
- [MSR-22] `getMatchDecisionsForPlaylist` added to `song-matching/decision-queries.ts` — symmetric counterpart to `getMatchDecisionsForSongs`; co-located with the existing decision query surface.
- [MSR-22] Playlist entitlement check is an ownership query (`playlist WHERE id AND account_id`) rather than an RPC — there is no `is_account_playlist_entitled` RPC in the DB; ownership is the correct gate for playlist-orientation subjects.
- [MSR-23] Migration filename: `20260625080000_msr23_capture_visible_pairs_rpc.sql` — applied locally; function verified in `pg_proc` with `prosecdef=true` and `search_path=public`.
- [MSR-23] `capture_match_review_item_visible_pairs_atomic` returns JSONB rather than TEXT — the `already_captured` idempotent path must return both a status and the original pair rows in one round-trip; a plain TEXT return cannot carry both; JSONB is the minimal extension that satisfies the acceptance criteria without a separate query.
- [MSR-23] `p_pairs` is passed as a plain JS array from the TypeScript client; Supabase JS serialises it to JSONB automatically — consistent with how `dismiss_match_review_item_atomic` passes its JSONB parameter.
- [MSR-23] Dense visible_rank validation uses `MIN=1, MAX=N, COUNT(DISTINCT)=N` rather than a loop — a single aggregate over `jsonb_array_elements` is sufficient and avoids iterating the array twice.
- [MSR-23] Subject consistency check is done after the `FOR UPDATE` lock rather than before — the subject IDs come from the locked row, so the check must follow the lock to avoid a TOCTOU window where the row changes between the check and the write.
- [MSR-23] Integration-only SQL-level assertions (dense ranks, duplicate pairs, subject mismatch, idempotency) are documented as skipped `it.todo` blocks in the test file — they require a live Supabase connection and cannot be unit-tested by mocking the client.
